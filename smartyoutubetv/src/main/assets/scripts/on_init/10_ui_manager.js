/**
 * Helps to add new buttons to the player's UI.
 */

console.log("Scripts::Running script ui_manager.js");

var UIManager = {
    init: function() {
        // do init here
    },
};

UIManager.init();